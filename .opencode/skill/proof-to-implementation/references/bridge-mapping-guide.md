# Bridge Mapping Guide

Map each approved proof claim to Rust behavior: `proof_claim_ref`, `rust_target`, concrete `source_refs`, independent `behavior_test_refs`, `refinement_harness_refs`, exact evidence command, workdir, artifact, and expected evidence.

State 7 may use `mapping_status: planned`; State 12 must close it as `materialized` or `verified`.
