# Waiver Execution Guide

Only `formal-verifier` writes `formal-waivers.jsonl`. Final `WAIVED` rows require `review_status: approved`, `status: approved`, candidate/formal canonical SHA-256 hashes, non-behavior proof, future expiry, boundary proof, compensating evidence, matching `ledger_result_ref`, and completed `proof-plan-reviewer` plus `formal-verifier` invocation provenance.
