# Behavior Test Rubric

Review public behavior, exact error variants, concrete assertions, boundaries, property/fuzz coverage, black-box integration tests, and scenario parity. Proof harnesses never count as behavior tests.
