# Determinism And Mutation

Reject ignored tests, sleeps, hidden shared mutable state, nondeterministic order, broad domain query mocks, stale snapshots, and tests that survive deletion of the behavior they claim to cover.
