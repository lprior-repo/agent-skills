# Trusted Base Writing Guide

Every assumption, stub, trusted boundary, disabled check, model reduction, bound, or unsupported path needs `trusted-base-ledger/v1`. Include concrete reason, scope, impact, compensating evidence, owner, expiry, and reviewer disposition.
