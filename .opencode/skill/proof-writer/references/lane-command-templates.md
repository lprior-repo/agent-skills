# Lane Command Templates

Use exact commands from `proof-obligations.planned.jsonl` first. Typical smoke commands: Verus target check, `cargo kani --harness`, `cargo flux`, `RUSTFLAGS="--cfg loom" cargo test <model>`, `cargo test <proptest>`, `cargo fuzz run <target> -- -runs=1000`.
