# Go-Skill Validator Fixtures

`fixture-manifest.json` defines the required fixture matrix and expected codes. Full fixture workspaces should be added under this directory and asserted by CI or a validator fixture runner. Static prose is not enough; every listed failure code must become executable fixture coverage.
