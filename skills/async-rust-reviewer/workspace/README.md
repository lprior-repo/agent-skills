This workspace keeps reusable async reviewer eval prompts under `evals/`.

Generated `iteration-*` run outputs are intentionally not retained in the skill snapshot. They are local historical artifacts, can contain stale reviewer wording, and are regenerated by running the eval harness. Do not treat missing iteration directories as deleted specifications.
