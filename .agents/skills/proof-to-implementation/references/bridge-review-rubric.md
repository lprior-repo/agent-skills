# Bridge Review Rubric

Reject TLA+ without Rust state/event mapping, file-only source refs, missing independent behavior tests, verifier harness reused as behavior test, missing refinement harness for verifier-backed rows, behavior waiver, or no evidence path.
