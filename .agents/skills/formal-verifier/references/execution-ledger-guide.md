# Execution Ledger Guide

Use `verification-ledger/v1`. Join by `obligation_id`, not ledger row `id`. `PASS` requires raw command evidence. `WAIVED` requires non-behavior formal waiver linkage. `FAIL_*` requires failing command evidence or explicit blocker evidence.
